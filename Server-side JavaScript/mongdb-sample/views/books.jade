extends layout

block content
  h1 Books in MongoDB
  table
    tr
      th ISBN
      th Title
      th Price
      for book in books
        tr
          td #{book.isbn}
          td #{book.title}
          td #{book.price}